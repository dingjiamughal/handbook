#!/bin/sh

# 初始化参数
# FE_PORT=${FE_PORT:-80}
# FE_PREFIX=${FE_PREFIX:-"/djtest"}
# FE_WORKER_PROCESSES=${FE_WORKER_PROCESSES:-8}


# 调整配置
# cd /etc/nginx/conf.d
# envsubst '\$FE_PORT \$FE_PREFIX' < nginx.tpl > default.conf
# sed -i -e "s/\(worker_processes\).*;/\1 $FE_WORKER_PROCESSES;/" /etc/nginx/nginx.conf

# 启动
# nginx
echo '123'